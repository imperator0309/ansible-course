---

- hosts: all
  become: true
  tasks:

  - name: update repository
    apt:
      update_cache: yes  
  - name: install apache2 package
    apt: 
      name: apache2
      state: latest
  - name: uninstall nginx packege
    apt:
      name: nginx
      state: absent
